Pilot Chirp 0.10 page actions on the characters roster route: creation now dispatches through `pages/characters/_actions.py` (`@action("create_character")` on the hidden `_action` form field), returning `FormAction` on success and a 422 `ValidationError` block re-render for htmx validation failures (plain POSTs keep the full-page error re-render). The `_actions.py` header documents the migration recipe for the remaining intent-dispatch routes.
