Story shelves, wanted hooks, world materials, and scene listings now use calmer surfaces, clearer spacing, and considered fallback artwork when realm media is missing. Board pages use open scene sections, text-led location links, quieter active filters, and shared spacing and elevation tokens; the composer has a more open writing surface, and the active-face selector submits through a CSP-safe listener. Community and sidebar links use a single soft accent line, topbar search keeps its input accessible without a segmented box, board hero counters no longer use framed letter tiles, director-only place controls use an open disclosure, hero copy fits narrow screens, and Studio Launch balances its readiness state with the opening controls.
