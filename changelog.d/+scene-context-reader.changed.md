Thread readers now include scene-context rails, mobile drawers for nearby scenes and grounding details, inherited scene media, and an editorial poster-wrap reading mode for character-forward posts.
Wide layouts dock grounded scene context beside the transcript (with a persisted toggle), grouped location lanes spotlight needs-attention beats, and desktop reply drafts stay surfaced with a sticky composer band.
The threaded scene lane adopts the mocked location vantage itself: eyebrow naming the sidebar realm, a headline anchored on the coarse place, descendant chips marching down to this room's queue, while the breadcrumb tray tucks aside on roomy layouts so shells do not preach the same geography twice.
The reader column echoes the mocked orientation band: coarse locale breadcrumbs, editorial sublines, light context chips for obligation casts and hooks, transcript resume separators when someone's read stamp trails new beats, composer topline reassurance, explicit mark-caught-up intent, and a caught-up badge driven by real unread markers instead of static optimism.
The mock-convergence pass moves the location scene lane into the shell's second rail, removes duplicate transcript/sections chrome from the reader body, and caps inherited scene media so it reads as one editorial image rather than a second hero.
Boosted route changes now swap the scene-aware second rail without a hard refresh, and the rail's location lane focuses on currently open scenes instead of mixing in paused threads.
Editorial post customization now stays quieter in the reader: character borders are suppressed until hover or focus so alternating posters and prose can flow together like the prototype.
Scene management and staff controls now live inside the scene context tray, keeping the reader body open for the editorial transcript while preserving the existing permission-gated forms.
The scene reader hero now uses a single scene-actions icon; watch, caught-up, latest-post, join, and operational controls sit in the context tray, while the top reply shortcut and generic scene-rule sentence were removed.
The desktop scene context panel now glides in from the right as an overlay with no top or bottom margin, matching the mocked tray behavior without compressing the transcript.
Inherited scene media now becomes the reader hero background behind the thread title, description, state chips, facets, and scene-actions button, removing the separate atmosphere media block and its boilerplate copy.
Editorial scene posts now use a tighter centered reading measure under the wide hero, preserving the mock's literary flow while keeping alternating character posters prominent.
The scene reply composer and after-scene continuation strip now match the tightened reader measure, with a compact WYSIWYG toolbar, shorter reply field, and slimmer continuation strip.
Editorial post metadata now keeps the author visible by default and reveals time, permalink, edit, and revision controls on hover or keyboard focus.
The after-scene continuation strip now follows the reply composer and offers a `Scenes here` return path alongside queue continuation links.
The scene-aware secondary rail now fills the available sidebar height and excludes the current thread from nearby-scene lists.
Scene-lane rows are more compact in the secondary rail, and `Open scenes` no longer includes scenes waiting on another writer after your own roster posted last.
Poster-style character profile cards in the editorial reader now open as hover/focus overlays instead of sitting below the poster by default.
The reply composer now makes the `Reply as` chip itself the character selector, removing the extra standalone dropdown from the topline.
The shell identity menu now uses a thinner active-face trigger, a compact face/writer dropdown, and click-away/Escape closing behavior.
