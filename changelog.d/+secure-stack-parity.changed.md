Development and production now run the same security middleware chain via Chirp's `secure_stack()` — sessions, CSRF, and security headers are active in every environment, the dev-only empty `csrf_field()`/`csrf_token()` stubs are gone (the framework globals render real session-bound tokens everywhere), and the session cookie's `Secure` flag resolves automatically from the deployment env.
