# Pre-commit hooks for Elbysodic.
# Install: uv run prek install
# Run manually: uv run prek run --all-files

default_language_version:
  python: python3.14

repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.15.1
    hooks:
      - id: ruff-check
        args: [--fix]
        types_or: [python, pyi]

  - repo: local
    hooks:
      - id: ruff-format
        name: ruff format
        entry: uv run poe format
        language: system
        types: [python]
        pass_filenames: false
      - id: ty
        name: ty type checker
        entry: uv run ty check src/elbysodic/
        language: system
        types: [python]
        pass_filenames: false
      - id: changelog-fragment-format
        name: changelog fragment format
        entry: python scripts/check_changelog_fragments.py
        language: system
        files: ^changelog\.d/.+\.(added|changed|deprecated|removed|fixed|security)\.md$
        pass_filenames: true

  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v5.0.0
    hooks:
      - id: check-added-large-files
        args: [--maxkb=1000]
      - id: check-case-conflict
      - id: check-merge-conflict
      - id: debug-statements
      - id: check-toml
      - id: check-yaml
        args: [--unsafe]
      - id: end-of-file-fixer
      - id: trailing-whitespace
        args: [--markdown-linebreak-ext=md]
      - id: check-executables-have-shebangs
      - id: detect-private-key
